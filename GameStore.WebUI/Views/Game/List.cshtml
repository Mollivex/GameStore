@using GameStore.WebUI.Models
@model GamesListViewModel

@{
    ViewBag.Title = "Products";
}

@foreach(var p in @Model.Games)
 {
    <div>
        <h3>@p.Name</h3>
        <p>@p.Description</p>
        <h4>@p.Price.ToString("# mdl")</h4>
    </div>
 }

